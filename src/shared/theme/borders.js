/*
 * Border-radius and border-width tokens.
 *
 * Mockup reference:
 *   sm   — small inner elements (progress bar, mini chips)
 *   md   — stat chips, tags
 *   lg   — standard cards (journey, expense, stat)
 *   xl   — large containers, bottom sheet top corners
 *   pill — badge pills ("Globetrotter Level 7")
 *   full — circular elements (FAB, avatar, tab active bg)
 */

export const BorderRadius = {
  sm:   8,
  md:   12,
  lg:   16,
  xl:   20,
  xxl:  24,
  pill: 100,
  full: 9999,
};

export const BorderWidth = {
  hairline: 0.5,
  thin:     1,
  medium:   1.5,
  thick:    2,
};
