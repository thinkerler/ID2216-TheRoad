/*
 * Typography presets matched to mockup text hierarchy.
 *
 * Mockup reference points:
 *   appTitle   — "The Road Goes Ever On" header bar
 *   pageTitle  — "My Journeys", "Dashboard"
 *   section    — "Community Insights", "Expense Categories"
 *   cardTitle  — city names "Tokyo", destination cards
 *   subtitle   — "Japan", "Kyoto, Japan"
 *   body       — descriptions, preference values
 *   caption    — secondary info, dates
 *   label      — "TRAVEL DATES", "DURATION" (uppercase)
 *   statLarge  — "12", "$24k" hero numbers
 *   statSmall  — "$2,450", "142" inline stats
 */

export const Typography = {

  appTitle: {
    fontSize: 17,
    fontWeight: '600',
    lineHeight: 22,
  },

  pageTitle: {
    fontSize: 28,
    fontWeight: '700',
    lineHeight: 34,
  },

  section: {
    fontSize: 22,
    fontWeight: '600',
    lineHeight: 28,
  },

  cardTitle: {
    fontSize: 18,
    fontWeight: '600',
    lineHeight: 24,
  },

  subtitle: {
    fontSize: 16,
    fontWeight: '500',
    lineHeight: 22,
  },

  body: {
    fontSize: 14,
    fontWeight: '400',
    lineHeight: 20,
  },

  caption: {
    fontSize: 12,
    fontWeight: '400',
    lineHeight: 16,
  },

  label: {
    fontSize: 11,
    fontWeight: '600',
    lineHeight: 14,
    letterSpacing: 1,
    textTransform: 'uppercase',
  },

  statLarge: {
    fontSize: 32,
    fontWeight: '700',
    lineHeight: 38,
  },

  statSmall: {
    fontSize: 20,
    fontWeight: '700',
    lineHeight: 26,
  },

  tagText: {
    fontSize: 13,
    fontWeight: '500',
    lineHeight: 18,
  },

  tabLabel: {
    fontSize: 10,
    fontWeight: '600',
    lineHeight: 14,
  },

  buttonText: {
    fontSize: 15,
    fontWeight: '600',
    lineHeight: 20,
  },
};
